query GetUsers {
  users {
    id
    userName
    firstName
    lastName
    email
  }
}

query GetUserById($id: String!) {
  user(where: { id: $id }) {
    id
    userName
    firstName
    lastName
    email
  }
}
